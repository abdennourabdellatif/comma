version: '3.8'

services:
  comma:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      APP_URL: ${APP_URL:-http://localhost:3000}
    volumes:
      - ./dist:/app/dist
    restart: unless-stopped
